# 语义星云多尺度重构计划

## 目标

- 保留星云的空间感、分类颜色、模型层切换、原始空间和证据抽屉。
- 消除 PCA 投影造成的片状挤压及离群点导致的主体压缩。
- 取消固定 320 个“大节点”与其余像素点之间的尺寸断层。
- 让任意小节点都能被靠近、选中、查看标签并进入证据。
- 在 5,000+ 节点下保持稳定帧率，不引入新的图形或布局依赖。

## 实施步骤

### 1. 稳健空间投影

- [x] PCA 坐标使用分位数而不是绝对最大值缩放，降低离群点压缩效应。
- [x] 在展示坐标层增加确定性的轴向展开与轻量密度松弛，缓解薄片和长带。
- [x] 原始 PCA 坐标保持不变，继续通过“原始空间”查看。
- [x] 添加确定性、坐标边界、离群点和邻域保持测试。

### 2. 连续节点尺度与动态 LOD

- [x] 删除固定 `DETAIL_NODE_LIMIT` 造成的两套尺寸体系。
- [x] 节点大小采用连续的平方根曲线，大小仅表达语义重力/证据支持度。
- [x] 根据相机距离、屏幕可见性和选中状态动态提升细节。
- [x] 缩放后让小点自然过渡为圆点、标签和关系线。

### 3. 可抵达的相机与导航

- [x] 扩大相机近距离范围。
- [x] 滚轮围绕鼠标所在语义位置缩放，而不是只朝画布中心推进。
- [x] 单击聚焦、双击深入，焦距根据节点及局部密度自动计算。
- [x] 增加放大、缩小、适配全图和返回主体按钮。
- [x] 保证视觉小点具有至少 12–16px 的命中区域，并解决重叠节点选择。
- [x] 相机运动可中断，普通操作过渡不超过 240ms；尊重 `prefers-reduced-motion`。

### 4. 信息层级与数据清理

- [ ] 总览优先显示语义区域和高证据节点，局部放大后展示长尾节点。
- [x] 标签按屏幕碰撞、证据支持度和当前焦点动态选择。
- [x] 过滤明显的提示词结构、字段计数和格式指令噪声。
- [ ] 低证据节点不删除，保留在深层视图和原始空间中。
- [x] 补充图例，明确距离、大小、亮度和颜色分别代表什么。

### 5. 验证与发布

- [ ] 单元测试覆盖投影、节点尺度、噪声识别和选择优先级。
- [ ] TypeScript、ESLint、完整测试及生产构建通过。
- [ ] 使用 5,000+ 节点数据验证：无尺寸断层、任意节点可聚焦、缩放不丢目标。
- [ ] 本地视觉验收后再提交、推送并通过 GitHub Actions 部署。

## 验收标准

- 相邻权重节点的显示半径连续，不因排名跨越某个固定阈值而突变。
- 离群节点不会把 90% 以上的普通节点压缩在狭窄星带中。
- 任意可见小点最多两次操作进入可读详情。
- 放大围绕指针位置进行，点击聚焦后节点保持在清晰观察区域。
- 优化布局与原始空间都可访问，视觉优化不冒充原始向量事实。
- 现有分类筛选、全屏、暂停、模型层及证据读取能力不回退。
